import 'package:flutter/material.dart';

class TipsDetailScreen extends StatelessWidget {
  const TipsDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
